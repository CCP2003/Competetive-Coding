class Solution:
    def reverse(self, x: int) -> int:
        n=str(x)[::-1]
        if n[-1]=='-':
            n='-'+n[:-1]
            n=int(n)
        else:
            n=int(n)
        if (-2**31)<=n<=((2**31) -1):
            return n
        return 0
